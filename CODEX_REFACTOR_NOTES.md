# 朝夕ボード：保守性改善版

添付された `index.html` / `notes.html` / `robots.txt` を基準にした差し替え用ソースです。GitHubのmainブランチへのpush、GitHub Pagesのデプロイ、Firebase本番データ・Security Rulesの変更は実行していません。

## 配置

ZIPを展開し、次のファイル・フォルダをリポジトリ直下へまとめて配置します。`index.html` / `notes.html` だけを差し替えると動きません。`assets/` も同じ更新に含めてください。

- `index.html`：Google認証付きのタスクボード・サイトメモ
- `notes.html`：サイトメモ・外注さんの共有ページ
- `robots.txt`：添付版と同一
- `assets/`：共通部品と各画面のJS/CSS
- `tests/`：依存パッケージ不要の検証コード。配信に必須ではありません

ビルド、npm install、フレームワーク、バンドラは不要です。Firebase compat SDKのバージョン（10.7.1）、公開設定、許可メールアドレス、相対URL、5列カンバン、日付選択、終礼まとめ、案件別メモ、外注プロフィールの構成を引き継いでいます。見た目の再設計は行わず、元のCSS宣言を基本に分離しました。実ブラウザによるピクセル比較は未実施です。

## 共通化した部分

| ファイル | 責務 |
| --- | --- |
| `assets/firebase-config.js` | 両ページで共有するFirebase Web設定 |
| `assets/workboard-core.js` | RTDBのFirestore風API、正規化、日付、並び順、競合検知、2回クリック削除確認、入力中DOMの保持 |
| `assets/records-ui.js` | メモ・プロフィール・外注ログの編集フォーム、コメント／ログ投稿、スレッド描画、イベント委譲 |
| `assets/records.css` | 編集・コメント部品の共通CSS。元のインライン装飾も移設 |
| `assets/index-app.js` / `assets/index.css` | タスクボードとメイン画面の接続・認証・表示制御 |
| `assets/notes-app.js` / `assets/notes.css` | 共有ページの案件選択・外注さん・認証・表示制御 |

`notes.html` も `collection().add/get/onSnapshot`、`doc().update/delete` を使います。Firebaseの `ref()` は共通アダプター内だけにあります。メモの編集・コメント機能はメイン画面にも同じ部品で反映しています。外注さんの機能は共有ページだけに残しています。

## 修正した不具合

1. **コメントの入力が反映されない**
   元の共有ページでは描画された `comment-text` / `comment-author` に対し、入力イベントが `c-text` / `c-author` を探していました。共通部品で同じ属性を描画・処理し、入力本文と記入者が送信されるよう修正。
2. **ライブ更新で入力・日本語変換が中断される**
   入力欄にフォーカスがある間はライブ更新によるDOM差し替えを保留。入力が終わると最新の内容を反映します。編集・投稿の下書きはレコードIDごとに保持します。別レコードの保存完了でも、今の下書きを消しません。
3. **古い編集内容で他の人の変更を上書きする**
   変更した項目だけを保存し、その項目が編集開始時から変わっていないかRTDBトランザクションで確認します。別項目の変更は保持し、同じ項目の競合は保存せず案内します。下書きは残ります。削除済みレコードも更新で再作成しません。
4. **タスクのログ追記で同時更新が消える**
   最新のログ配列へトランザクション内で追記する方式に変更。配列のフィールド名・データ形式は同一です。
5. **二重送信・確認タイマーの残留**
   保存／送信中の多重実行を防止。削除の2回クリック確認を共通化し、対象・画面の切替、ログアウト、ページ離脱時に解除します。次回依頼日の「保存しました」タイマーも対象IDごとに管理し、後片付けします。
6. **外注さん切替後に別の人へ保存状態が反映される**
   プロフィール・ログ編集・次回依頼日の状態をIDに紐づけて管理。次回依頼日も競合を確認して保存します。保存失敗を画面上に表示します。
7. **匿名認証が別タブのGoogleログインを置き換える**
   共有ページは既存の認証状態を再利用し、未認証の場合だけ匿名認証します。メイン画面は匿名ユーザーにログイン画面を表示しますが、共有ページで使っている匿名セッションを強制ログアウトさせません。タスク側の許可メール制はそのままです。
8. **認証切替・ページ離脱後に購読が残る**
   各購読を解除し、古い接続から遅れて届くコールバックを無効化。ログアウト時はタスク・履歴の表示も消去します。
9. **日付・既存ヘルパーの不整合**
   共有ページの日付表示をメイン画面と同じローカル日付に統一。旧メモ移行ヘルパー内の未定義 `showToast` を修正し、既に移行済みのメモを再実行で上書きしないようにしました。移行は自動実行しません。
10. **操作可能な欄が読み上げ対象外／スマホで追加不可**
    共有ページの案件・外注さん選択欄から誤った `aria-hidden` を除去。スマホでも既存の外注追加操作に到達できるよう「＋」を表示しました。

## データ互換性

読み書き先と保存フィールドを変更していません。

| コレクション | 用途・保存形式 |
| --- | --- |
| `tasks` | 既存タスクフィールドとログ配列。`createdAt` / `doneAt` の日次ビューの扱いも維持 |
| `siteMemos` | `projectTag,text,date,author,createdAt` |
| `siteMemoComments` | `memoId,text,author,createdAt` |
| `staffProfiles` | `name,profile,currentWork,nextRequestDate,updatedAt` |
| `staffNotes` | `staffId,text,author,createdAt` |
| `siteNotes` | 旧データ。通常画面では使わず、既存の手動移行ヘルパーだけが参照 |

`_raw` は読み込んだ元データを編集開始時の比較用に保持するメモリ内プロパティです。Firebaseへ書き込みません。編集時に新しいバージョン番号フィールド等を追加する必要はありません。

トランザクションの実装は、Firebase公式の[データの読み書き・トランザクション](https://firebase.google.com/docs/database/web/read-and-write#save_data_as_transactions)にある仕組みを使っています。トランザクションは対象パスへのread/write権限が必要です。

## 検証

Node.js 18以降があれば、プロジェクト直下で実行できます。これらは任意の開発時検証であり、公開前のビルド処理ではありません。

```sh
node tests/static-check.cjs
node --test tests/workboard.test.cjs
```

実行結果：静的検査成功、17テスト成功。

対象：コメントの入力イベントと二重送信、保存失敗時の下書き保持、同時編集・削除競合、ID別の編集状態、タスクログの同時追記、削除確認タイマーの解除、購読解除、入力中DOMの差し替え保留、日付と案件表記、手動移行の再実行、両ページの初期化と認証・離脱時の後片付け。

検証はメモリ上の模擬RTDBと最小DOMスタブで行っています。Firebase Emulator、実DB、Google認証ポップアップ、実ブラウザ上のレイアウト／日本語入力、GitHub Pagesへの配信は未検証です。テストは実際のSecurity Rulesの許可を証明するものではありません。

## 今回変更していない運用と限界

- 匿名認証を許可した共有データの権限は変更していません。共有範囲の制限を強める場合は、URLの秘匿とは別にFirebase側のRules設計が必要です。
- 履歴データ全件を購読する既存方式を維持しています。大量データのページングやサーバー検索は追加していません。
- コメント／外注ログは親の存在を確認して追加しますが、親削除と追加が完全に同時の場合の参照整合性を、複数パスをまたいで保証する変更は行っていません。厳密な保証にはRulesまたはバックエンド側の対応が必要です。
- 日次ビューは既存のフィールドに基づく表示で、過去時点の全変更履歴を復元する監査ログには変更していません。

差し替え前にリポジトリ側へ別の更新が入っている場合は、添付版との差分を確認してから取り込んでください。
