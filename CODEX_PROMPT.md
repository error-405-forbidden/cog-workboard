# 朝夕ボード（cog-workboard）改善依頼プロンプト（3回目：サイバー感の強化）

前回、`assets/dashboard.css`という追加専用のCSSファイルで「ダークダッシュボード」テーマを実装済み（既存の`index.css`/`notes.css`/`records.css`はそのまま、後読みで上書きする方式）。今回は**そのサイバー感をもっと強く**したい。方向性は「フルサイバーパンクHUD」。ロジック・データ構造は今回も変えない。

## 重要：必ず最新のmainブランチを基準にすること
前回、Codexが生成したHTML/JSが**最新mainより古いスナップショット**を基準にしていて、後から追加された「外注さん」タブの機能がindex.html側で丸ごと消えてしまう事故が起きかけました（コミットは指定通りと申告されていたが、実際の中身は古いままだった）。このため今回は：

- **HTML/JS本体（`index.html`、`notes.html`、`assets/index-app.js`、`assets/notes-app.js`、`assets/workboard-core.js`、`assets/records-ui.js`）には一切触れないこと。**
- 変更は**新しいCSSファイルを1つ追加し、既存の`<link>`タグの後にもう1行`<link>`を追加するだけ**に限定する（前回の`assets/dashboard.css`と全く同じやり方）。
- これにより「古いスナップショットを基準にしてしまうリスク」自体を構造的に排除する。HTMLの中身を書き換える必要はまったくないはず。

## これは何か
社内向けタスク管理ツール「朝夕ボード」。GitHub Pages + Firebase Realtime Database + Firebase Authenticationで動く、ビルド不要の素のHTML/JS/CSS（フレームワーク・バンドラなし）。

- 公開URL: https://error-405-forbidden.github.io/cog-workboard/
- リポジトリ: https://github.com/error-405-forbidden/cog-workboard（mainブランチ直下がそのままGitHub Pagesで配信される）
- `index.html` — メインツール（Google認証）。タスクボード／サイトメモ／外注さんの3タブ。
- `notes.html` — サイトメモ／外注さんだけの共有ページ（匿名認証・ログイン画面なし）。
- `assets/dashboard.css` — 前回追加した「ダークダッシュボード」テーマ。両ページで`assets/records.css`の後に読み込まれている。CSSカスタムプロパティ（`--bg`、`--accent`、`--violet`等）と主要コンポーネントのクラス（`.btn`、`.card`、`.project-note`、`.staff-profile`、`.view-nav`等）をひととおり定義済み。**この内容はそのまま活かして、上に重ねる／一部を上書きする形で拡張してほしい**（ゼロから書き直さなくてよい）。

## 今回のお願い：サイバー感を強化（フルサイバーパンクHUD）

現状の`dashboard.css`は「落ち着いたダークダッシュボード」止まりで、もっと"サイバー"にしたい。以下の要素を、新しい追加CSSファイル（例: `assets/cyber.css`）として実装してほしい。`assets/dashboard.css`の後に読み込む。

### 具体的に足したい要素
1. **発光の強化**: ボタン・アクティブなタブ・カード・パネル境界線の発光（box-shadowのぼかし・広がり）を今より強く。ホバー時はさらに強調。
2. **角の切り欠き（HUD風コーナー）**: 主要パネル（`.quickadd`、`.notes-projects`、`.staff-profile`、`.project-note`、`.card`、`.column`、`dialog`、`.login-box`等）の角を`clip-path`で斜めに切り欠き、SFの管制端末っぽい輪郭にする。あるいは、四隅に直角のコーナーブラケット（`⌐`のような角マーク）を`::before`/`::after`で配置する方式でもよい（どちらが実装しやすいか判断して選んでよい）。
3. **セカンダリのネオンカラー追加**: 既存のシアン（`--accent`）に加えて、マゼンタ／ピンク系のネオンカラーを装飾用に追加（例: コーナーブラケットや区切り線、強調ラベルなど）。**既存の意味を持つ色（`--amber`=警告・保留、`--red`=エラー・期限超過、`--green`=完了、`--violet`=ルーティン表示）の意味は変えないこと**。マゼンタは純粋に装飾用。
4. **スキャンライン背景**: 前回はうっすらしたグリッド背景のみ。これに加えて、ごく薄い水平スキャンライン（CRTモニターっぽい横線）のオーバーレイを追加。情報の可読性を落とさない透明度にすること。
5. **ホバー時の軽いグリッチ演出**: 見出しやボタンにホバーした時、ごく短時間・ごく軽微なテキストグリッチ（RGBズレ）を入れる。**やりすぎ注意**：一瞬で終わる、頻繁に誤操作を誘発しない、読みにくくしないこと。`prefers-reduced-motion: reduce`では完全に無効化。
6. **接続ステータスまわりの演出強化**: 「接続済み」のドットや`.connection`表示を、システム稼働中のHUDらしく、もう一段際立たせる（発光・パルスの強化など）。
7. **等幅フォントの使用範囲を拡大**: 前回は日付・件数など数値系のみ等幅にした。今回はボタンラベルや見出しなど、もう少し広い範囲で等幅フォント（システムにある`ui-monospace`系）を使い、ターミナル／HUDらしい統一感を出す（本文の説明文などは今まで通り読みやすいフォントのままでよい）。

### 制約（前回と同じ）
- **見た目以外は変えない**: JSのロジック・関数シグネチャ・Firebase設定・データ構造・URLは一切変更しない。
- **HTML/JSファイルは一切編集しない**。新しいCSSファイルの追加と、`<link>`タグの追加1行×2ファイルのみ。
- `assets/dashboard.css`・`assets/index.css`・`assets/notes.css`・`assets/records.css`は変更しない（新しいCSSで上書き・拡張する）。
- 情報密度・レイアウト構造・5列カンバン・案件一覧＋詳細の2カラム等は維持。**再設計ではなく装飾の強化**にとどめること。
- アクセシビリティを壊さない：コントラスト比、`:focus-visible`のアウトライン、`aria-*`属性、`forced-colors`対応はそのまま機能すること。
- `prefers-reduced-motion: reduce`、モバイル（800px以下）でのアニメーション・重い演出の抑制は前回同様に維持・拡張すること。
- 外部フォント・画像・アイコンライブラリの追加は不要（システムフォント・CSSのみで表現）。
- `node tests/static-check.cjs` と `node --test tests/workboard.test.cjs` が全通過することを確認してから納品すること（HTMLに新しいidやscriptタグを増やしていなければ、通常はそのまま通るはず）。

### 成果物・報告してほしいこと
- 追加したファイル一覧（新CSSファイル＋`<link>`追加箇所）
- 実際に加えた演出の説明（コーナーブラケットの実装方式、グリッチの発火条件・時間、色の追加箇所など）
- テスト実行結果
- 追加ファイルのサイズ（前回同様、軽量性を意識していることの根拠として）

不明点や、やりすぎ・読みにくくなるリスクがある判断は、実装前に確認してください。
