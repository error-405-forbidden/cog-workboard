# 朝夕ボード（cog-workboard）改善依頼プロンプト（2回目）

前回、コードの保守性リファクタ（`assets/`への分割、複数の実バグ修正、テスト追加）を実施済み。今回は**見た目のリニューアル**が主目的。ロジック・データ構造は変えない。

## これは何か
社内向けタスク管理ツール「朝夕ボード」。GitHub Pages + Firebase Realtime Database + Firebase Authenticationで動く、ビルド不要の素のHTML/JS/CSS（フレームワーク・バンドラなし）。

- 公開URL: https://error-405-forbidden.github.io/cog-workboard/
- リポジトリ: https://github.com/error-405-forbidden/cog-workboard（mainブランチ直下がそのままGitHub Pagesで配信される）

## 現在のファイル構成（前回リファクタ後＋今回の追加分）
- `index.html` — メインツール。Google認証（許可メールアドレスのみ）でログイン。3タブ構成：**タスクボード**（5列カンバン＋日付ピッカーの日次ビュー）、**サイトメモ**（案件ごとの申し送り事項タイムライン）、**外注さん**（外注スタッフ1人=1プロフィールページ、プロフィール／依頼している内容／次回依頼日／ログのスレッド）。
- `notes.html` — 上記のうち「サイトメモ」と「外注さん」だけを切り出した共有用ページ。ログイン画面なし（Firebase匿名認証を自動実行してからDBに接続、既存のGoogleログインセッションがあればそれを再利用）。リンクを知っている人なら誰でも閲覧・編集・コメントできる想定。
- `robots.txt` — サイト全体を`Disallow: /`（検索インデックス対策、変更不要）。
- `assets/firebase-config.js` — 両ページ共通のFirebase Web設定。
- `assets/workboard-core.js` — RTDBのFirestore風API、正規化、日付、並び順、競合検知（トランザクション）、2回クリック削除確認、入力中DOMの保持。**ロジックのみ、変更不要**。
- `assets/records-ui.js` — メモ・外注プロフィール・外注ログの編集フォーム、コメント／ログ投稿、スレッド描画、イベント委譲を1つのコードで両ページに提供する共通部品。**ロジックのみ、変更不要**（描画するHTML断片のクラス名は今回のCSS刷新に合わせて変えてよいが、`data-wb-action`等のイベント委譲用属性は壊さないこと）。
- `assets/index-app.js` / `assets/index.css` — メイン画面（タスクボード・サイトメモ・外注さん）の接続・認証・表示制御。
- `assets/notes-app.js` / `assets/notes.css` — 共有ページ（サイトメモ・外注さん）の接続・認証・表示制御。
- `assets/records.css` — 編集・コメント・ログスレッド部品の共通CSS（両ページで共有）。
- `tests/` — Node標準テストランナーのみで動く検証コード（`node tests/static-check.cjs`、`node --test tests/workboard.test.cjs`）。**必ず全通過を維持すること**。特に`static-check.cjs`は「`<style>`/`<script>`をHTMLに直書きしない」「`index-app.js`/`notes-app.js`内の`$('id')`参照がHTMLの実際のidと一致する」ことを機械的に検査している。

## データ構造（変更しないこと）
Firebase Realtime Databaseのコレクション名・フィールド名は一切変えない：`tasks`、`siteNotes`（旧データ、読み書きなし）、`siteMemos`、`siteMemoComments`、`staffProfiles`、`staffNotes`。Firebase設定・許可メールアドレス（`ALLOWED_EMAILS`）もそのまま。

## 今回のお願い：見た目のリニューアル

### 現状の課題
今のデザインは落ち着いた緑系トーン（`--accent:#176859`など）で、機能面は整っているが「もっさりして見える」というフィードバックを受けた。もっとテクノロジー感のある、かっこいいUIにしたい。

### 方向性：ダークダッシュボード
SFの管制室・監視ダッシュボードのような雰囲気。以下を基準にしつつ、デザイナーとしての判断で細部は調整してよい。

- **配色**: 深い黒緑・黒紺系の背景（例: `#0a0e14`前後）＋ネオン系アクセント（シアン系 `#00e5c7`前後をプライマリ、バイオレット系 `#7c5cff`前後をセカンダリ／強調用）。現状のライト/ダーク両対応（`prefers-color-scheme`）は維持してよいが、**このツールはダークを基本形として設計し直してよい**（ライト側は簡易的なフォールバックで構わない）。
- **パネル**: 半透明ガラス風（`backdrop-filter: blur(...)`）＋細い発光ボーダー。カード・ダイアログ・サイドバーなどの「箱」に統一感を持たせる。
- **タイポグラフィ**: 見出しは幾何学的なサンセリフ（例: Space Grotesk、Sora等、Google Fontsから読み込んで良い）。日付・件数・IDなど数値的な要素は等幅フォント（例: JetBrains Mono、Fira Code）でタブular-nums、ダッシュボードらしい精密さを出す。本文（タスクの説明文など）は今まで通り読みやすい可変幅フォントのままでよい。
- **ボタン・カード**: 角丸を今より控えめに（現状`--radius:12px`前後→もっとシャープに）。ホバー・フォーカスで発光するようなアクセント（box-shadowのglow等）。
- **接続ステータス**（`.connection`のドット等）: 既にある「接続中／接続済み／エラー」の状態表示を活かし、接続済み時にパルスするアニメーションを加えるなど、システム稼働中らしい演出を足す。
- **背景装飾**: 薄いグリッド線やスキャンラインなど、ダッシュボードらしい背景テクスチャを控えめに（情報の可読性を絶対に落とさないこと）。
- **アニメーション**: ビュー切り替え・カード出現・ボタンホバーなどに150〜250ms程度の滑らかなトランジション。`prefers-reduced-motion`は今まで通り尊重すること。

### 制約
- **見た目以外は変えない**: JSのロジック・関数シグネチャ・Firebase設定・データ構造・URLは一切変更しない。CSS（`assets/*.css`）を中心に、必要なら`index.html`/`notes.html`のクラス名やごく軽微な装飾用マークアップ（背景装飾のdivなど）の追加程度に留める。
- **`assets/workboard-core.js`と`assets/records-ui.js`のロジック部分は変更しない**（クラス名の変更で見た目に反映される範囲はOK）。
- タスクボード・サイトメモ・外注さんの3画面、`index.html`・`notes.html`両方に一貫したデザインを適用すること。
- 情報密度・レイアウト構造（5列カンバン、案件一覧＋詳細の2カラム、日付ピッカー等）は維持し、**再設計ではなく見た目の刷新**にとどめること。
- アクセシビリティを壊さない：コントラスト比、`:focus-visible`のアウトライン、`aria-*`属性はそのまま機能すること。
- `node tests/static-check.cjs` と `node --test tests/workboard.test.cjs` が全通過することを確認してから納品すること。
- Google Fontsを使う場合、読み込み元URLを明記すること（オフライン環境での挙動やCSP等は気にしなくてよい）。

### 成果物・報告してほしいこと
- 変更したファイル一覧
- 実際に何を変えたか（配色・フォント・コンポーネントごとの変更点）を文章で説明（実ブラウザでのスクリーンショット比較は今回も未実施の前提でよいが、CSSの変更点は具体的に）
- テスト実行結果
- 今回さらに手を入れる余地がある箇所があれば提案（実装は不要、コメントのみでよい）

不明点や、デザイン上の判断に迷う点があれば、実装前に確認してください。
